{
  "name": "clfc-wheel",
  "version": "1.0.0",
  "description": "CLFC Spinning Wheel production build",
  "main": "_worker.js",
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
